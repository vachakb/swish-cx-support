# Server
PORT=8787

# LLM provider: "gemini" or "mock".
# Leave LLM_PROVIDER empty to auto-select: gemini if GEMINI_API_KEY is set, else mock.
LLM_PROVIDER=
GEMINI_API_KEY=

# Database (libSQL / local SQLite file)
DATABASE_URL=file:./data/swish.db
